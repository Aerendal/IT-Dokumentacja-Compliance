V1 closure summary:

Safe autofixes covered end-to-end:
  DOC.SECTION.MISSING  — inserts placeholder section at document end
  DOC.EMOJI.FORBIDDEN  — strips emoji characters, preserves all other content

Report-only (not auto-modified):
  DOC.FRONTMATTER.MISSING   — requires manual decision
  DOC.FRONTMATTER.NO_TITLE  — requires manual decision

V1 limitations documented in docs/DEV_WORKFLOW.md.

Idempotency confirmed: second analyze after apply shows Safe(auto)=0.

apply_result.json format: schema_version, run_id, changed, skipped, errors, files[].
