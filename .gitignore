# Large generated content (not tracked by default)
# Note: generated_templates/core/ and demo subdirs ARE explicitly tracked via git add
# generated_templates/ line removed — tracked content must be visible
exports/
mutants/

# Database files
reports/
it_doc_matrix.db
*.db
*.db-shm
*.db-wal
*.db.*backup*

# Python artifacts
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env
.env.*

# Coverage / test artifacts
.coverage
.pytest_cache/
htmlcov/
coverage.xml

# Large IT doc text files
IT_Dokumentacja_Czesc_*.md
odpow*.md

# Session artifacts and local packs
*.tar.gz
*.zip
*.sqlite
*.sqlite3

# All reports (local runtime artifacts, not part of public contract)
reports/

# Root-level generated/session documents
Cykl-Produkcyjny.md
hdfs_spec.md

# Scripts with hardcoded local paths (not portable)
scripts/extract_requirements.py
scripts/extract_requirements_workshop.py
